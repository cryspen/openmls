#+TITLE: OpenMLS treemath — panic-freedom proofs (Aeneas → Lean)
#+OPTIONS: toc:2

* Overview

This directory holds the Lean 4 / [[https://github.com/AeneasVerif/aeneas][Aeneas]] proof development for the *ratchet-tree math* of
OpenMLS (RFC 9420 §4 / Appendix C, ported to Rust in
=openmls/src/binary_tree/array_representation/treemath.rs=).

The Rust functions carry =hax_lib= annotations (=requires= / =ensures=); Charon extracts them to
LLBC and Aeneas to Lean. We discharge, in Lean, the proof obligations Aeneas generates: each
function is panic-free on its stated precondition and meets its stated postcondition.

|---------------------------------+-----------------+-------------+-----------------------------------------------------------------|
| Obligation                      | Reference post  | Status      | Notes                                                           |
|---------------------------------+-----------------+-------------+-----------------------------------------------------------------|
| =level=                           | real (char.)    | proved      | via =level.spec_pure= (registered); official proof derived        |
| =root=                            | real            | proved*     | *statement still has weakened =True= post; restate pending        |
| =left=                            | real            | proved*     | same caveat; green on the pure level spec                       |
| =right=                           | real            | proved*     | same caveat                                                     |
| =is_node_in_tree=                 | =True=            | proved      |                                                                 |
| =LeafNodeIndex.to_tree_index=     | real            | proved      |                                                                 |
| =LeafNodeIndex.from_tree_index=   | =True=            | proved      |                                                                 |
| =ParentNodeIndex.to_tree_index=   | real            | proved      |                                                                 |
| =ParentNodeIndex.from_tree_index= | =True=            | proved      |                                                                 |
| =TreeNodeIndex.new=               | real            | proved      | provable only after the =MAX_TREE_INDEX= off-by-one fix           |
| =TreeNodeIndex.u32=               | real            | proved      |                                                                 |
| =TreeSize.leaf_count=             | real            | proved      |                                                                 |
| =TreeSize.new=                    | real            | parked      | scaffolded; =Nat.log=/=2^e= residue sorried, dead ends documented |
| =TreeSize.inc=                    | real            | parked      | one case proved; goal inventory in comments                     |
| =TreeSize.dec=                    | real            | parked      | 2 leaves from done, both with known proofs (see comments)       |
| =sibling=                         | =True=            | open        | depends on =parent='s spec                                        |
| =direct_path=                     | real (2×∀)      | open        | needs loop invariant + =parent='s post                            |
| =copath=                          | =True=            | open        | needs =direct_path= + iterator contracts (trust-surface gate)     |
| =parent=                          | real (3-clause) | QUARANTINED | "blows up"; split =spec_pf=/=spec_fc=; blocks the 3 above         |
| =lowest_common_ancestor=          | =True=            | QUARANTINED | "blows up"                                                      |
| =common_direct_path=              | =True=            | QUARANTINED | "blows up"; its loop lemma also sorried (draft in-file)         |
|---------------------------------+-----------------+-------------+-----------------------------------------------------------------|

Loop specs: =level_loop_spec= proved, =lca_loop0_spec= proved,
=common_direct_path_loop_spec= sorried (commented draft in-file), =direct_path_loop= /
=copath_loop= not yet stated.

** Contributions

- Change the ~MAX_TREE_SIZE~ constant https://github.com/openmls/openmls/pull/2136

* High-level tasks

** TODO Empty the =MissingCoreSpecs.lean=

Open a PR with those specs

** TODO Upstream fix for looping ~scalar_tac~

** Update specs

* For AI agents

- =Openmls/Proofs/HANDOFF.md= — the consolidated session handoff (state, architecture,
  playbook, pending decisions). Single source of truth; superseded episode handoffs deleted.
- Root =CLAUDE.md= — the broader FV-target roadmap and pipeline.

** Module layout

Hand-written proof modules under =Openmls/Proofs/=, import DAG
=Common ← BitMath ← {MissingCoreSpecs, AdmittedCoreSpecs} ← Proofs=:

| File                   | Concern                                                                                     |
|------------------------+---------------------------------------------------------------------------------------------|
| =Common.lean=            | generic loop driver =loop_spec_measure= + triple helpers, =vecLen=                              |
| =BitMath.lean=           | pure-=Nat= =tones= / =parent= / shift / =Nat.log= lemmas; the =scalar_tac=/=simp= registrations       |
| =MissingCoreSpecs.lean=  | PROVED =@[spec]= contracts for =core=/=alloc= ops (=u32_pow_spec=, =leading_zeros_spec=, …)         |
| =AdmittedCoreSpecs.lean= | TRUSTED audit surface: 12 admitted =@[spec]= core contracts                                   |
| =Proofs.lean=            | the obligations, loop specs, =level.spec_pure=, the raw =attribute [spec]= helper registrations |

Generated (frozen — hand-overridden, do not regenerate without re-applying overrides):
=Openmls/Extraction/{Types,Funs,Specs}.lean=. Hand-derived reference:
=Openmls/Extraction/ProofObligationsExplicit.lean= (and the Aeneas-generated
=ProofObligations.lean= it mirrors).

Upstream-issue reproducers live in =Openmls/Issues/= (one issue per file, intentionally
failing to compile, imported by no build target — see its =README.md=). Current entries:
the two =scalar_tac= non-termination reproducers (=ScalarTacNatLogLoop.lean=,
=ScalarTacCyclicHyps.lean=).

** The proof scheme

- *One theorem per obligation.* =Openmls/Extraction/ProofObligationsExplicit.lean= is the
  hand-derived reference view of all *21* obligations in explicit Hoare-triple form
  (=(pre).holds → ⦃⌜True⌝⦄ f args ⦃⇓ res => (post).holds / True⦄=). =Proofs.lean='s theorem
  statements must correspond 1:1 to it (same names, same triples). The reference file is not
  imported; its proofs stay =sorry=.
- *Stepping* is done by =hax_mvcgen= (fallback: plain =mvcgen= + local massaging at spans where
  it misbehaves). After stepping, residual goals must be pure scalar/bit arithmetic.
- *Loop invariants* are stated separately, one =*_loop_spec= theorem per loop via the generic
  =loop_spec_measure= driver in =Common.lean=.
- *Pure-spec exception (validated):* =level='s monadic =level.post= was too costly to re-step at
  every use site (it appears inside the posts of =parent=/=left=/=right=). =level.spec_pure=
  states the trailing-ones characterization as a pure =Prop= with a hypothesis pre; it carries
  the =@[spec]= registration, and the official =level.spec.proof= is derived from it
  (unregistered). New pure specs require explicit user validation, case by case.
- *Sorry style:* a remaining =sorry= sits under its own explicit bullet / =case= block with a
  =-- TODO(arith): …= comment; a bare =all_goals sorry= is allowed only when *every* remaining
  goal is sorried. No =sorry= may hide inside a =first | … | sorry= chain, and no goal
  containing a Hoare triple / =wp⟦…⟧= may be sorried (that indicates broken stepping).

** TODO

*** Close the parked TreeSize leaves
- [ ] =TreeSize.dec= — 2 leaves, both with written-out proofs (=vc1.h=: =MIN_TREE_SIZE= value +
  omega; =vc3.h_ok.pre=: copy of =inc='s proved =vc11.h_ok.pre= block, no exponent shift), then
  =all_goals scalar_tac= closes the rest. Beware duplicated case tags across the two
  =leading_zeros= branches — use the order-free =all_goals try (<script>)= form.
- [ ] =TreeSize.inc= / =TreeSize.new= — same technique; goal inventories in the proof comments.

*** Unblock the quarantined proofs
- [ ] =parent= (=spec_pf=/=spec_fc=) — the keystone: =sibling=, =direct_path=, =copath= all
  consume its spec. If the blow-up is the monadic-post problem =level= had, a validated
  =parent.spec_pure= (value equation via =tones=, validity, level-successor clause) is the
  highest-leverage candidate fix.
- [ ] =lowest_common_ancestor= — rebuild on spec-based style (currently pre-migration).
- [ ] =common_direct_path= + its loop lemma (draft proof in-file).

*** Restate the weakened posts
- [ ] =root=, =left=, =right= still state =⇓ res => True= in =Proofs.lean=; the reference posts
  are real. Restate + re-prove (proofs should mostly carry over on the pure level spec).

*** Remaining scaffolding
- [ ] =direct_path_loop_spec= + named invariant; =copath_loop_spec=.
- [ ] =copath= iterator tail: needs admitted =map=/=collect= contracts — user gate before adding.

*** Shrink the trusted audit surface
- [ ] =into_map_collect_spec= — provable with faithful Lean models instead of bare axioms.
- [ ] =vec_push_spec= — provable from =seq_push=; blocked on the =vecLen= vs CoreModels-=Seq=
  representation mismatch.
